#!/bin/sh /etc/rc.common

START=82
APP=dlep
PID_FILE=/var/run/$APP.pid

start() {
	start-stop-daemon -S -x $APP -p $PID_FILE -m -- -l /etc/dlep.conf
}

stop() {
	start-stop-daemon -K -n $APP -p $PID_FILE -s TERM
	rm -rf $PID_FILE
}
