# This is an example of a custom settings file
# run "controller-admin.sh install_requirements" and
# uncomment the following lines if you want to run a production-like controller


#from controller.utils import add_app, remove_app

#from settings import *


#INSTALLED_APPS = remove_app(INSTALLED_APPS, 'vct')
#INSTALLED_APPS = add_app(INSTALLED_APPS, 'registration', prepend=True)
#INSTALLED_APPS = add_app(INSTALLED_APPS, 'communitynetworks')
#INSTALLED_APPS = add_app(INSTALLED_APPS, 'issues')
#INSTALLED_APPS = add_app(INSTALLED_APPS, 'gis')
#INSTALLED_APPS = add_app(INSTALLED_APPS, 'captcha')
#INSTALLED_APPS = add_app(INSTALLED_APPS, 'django_google_maps')

